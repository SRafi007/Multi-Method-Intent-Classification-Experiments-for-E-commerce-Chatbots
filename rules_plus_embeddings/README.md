# Rules + Embeddings Hybrid Intent Classifier

## How it Works

1. **Rules Layer**
   - Keyword match
   - Regex match
   - Immediate, high-precision results

2. **Embedding Fallback**
   - Uses MiniLM embeddings
   - Matches intent names by semantic similarity
   - Ensures coverage for ambiguous queries

## Run Inference
